locals.
	not-overwritten: (third) Before 'extends', dash in key
	page:
		title: A third page
		description: The third page has even more information

extends(src='layout.sgr')
	block(name='title')
		title {{ (item || page).title }}

	block(name='content')
		p {{ `${not-overwritten}` }}
