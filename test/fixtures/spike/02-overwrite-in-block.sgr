locals.
	page:
		title: A second page
		description: Second page has some more information

extends(src='layout.sgr')
	block(name='locals')
		locals.
			overwritten: (second) Inside 'extends'

	block(name='title')
		title {{ page.title }}

	block(name='content')
		p {{ overwritten }}
